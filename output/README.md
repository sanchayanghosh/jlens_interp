# Generated outputs

The driver writes labelled rollout groups here as JSONL, plus a neighboring `*.summary.json`.
Partial JSONL files remain valid if a run is interrupted and can be continued with `--resume`.
